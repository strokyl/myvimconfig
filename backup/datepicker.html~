<input
    id="{{inputId}}"
    name="{{name}}"
    type="text"
    class="form-control" 
    data-ng-model="_date"
    data-is-open="dateOpened"
    data-ng-required="true"
    data-close-text="{{closeText}}"
    data-datepicker-popup="{{format}}"
    data-init-date="{{initDate}}"
>

<span class="input-group-btn">

    <button
        type="button"
        class="btn btn-default"
        data-ng-click="openDate($event)"
        >
        <i class="icon-af icon-af-A-basic-calendar52"></i>
    </button>

</span>
