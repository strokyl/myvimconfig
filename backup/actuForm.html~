<div page-access-keys="AK_ADMIN" class="container">
    <div ng-show="message.length" class="bg-danger">{{message}}</div>
    <br>
    <p ng-hide="actu" class="has-error" translate>actuForm.error.notfound</p>
    
    <h1 ng-if="!createActu">{{actu.actuFile}}.dcr</h1>
    <h1 ng-if="createActu" translate>actuForm.newActu</h1>
    


    <form role="form" class="form-horizontal" ng-if='actu' ng-submit='saveChange()'>

        <!-- Génération de html du formulaire -->
        <myform
            data-form='form'
            data-resource='actu'
        />

        <!--  Bouton annuler et supprimer -->
        <div class="form-group">
            <div class="col-sm-4 col-sm-offset-8">

                <button type="submit" translate class="btn btn-primary">generic.save</button>
                <button type="button" translate class="btn btn-primary" ng-click='apercu()'>actuForm.apercu</button>
                <button type="button" translate class="btn btn-primary" ng-click='publish()'>actuForm.publish</button>
                <button type="reset" translate class="btn btn-danger" ng-click='cancel()'>generic.cancel</button>
            
            </div>
        </div>

    </form>







</div>
